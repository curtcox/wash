#!/usr/bin/env python3
import json
from pathlib import Path

names = []
for path in sorted((Path.cwd() / "env" / "meta").glob("*")):
    if path.is_file():
        names.append(path.name)
print(json.dumps({"commands": names, "hint": "Open /ui/<path> to frame a raw wash URL."}, indent=2))
